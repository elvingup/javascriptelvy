// criar variavel na forma antiga
var nomeVariavel;

// metodo novo baseado no ES6
let nomeVar;
let varComValor = 10;

// ESCOPO

// constantes
const nomeConstante = 111;

// literais
let dadosLiterais = `
<ul>
    <li>${varComValor}</li>
    <li>${(cond) ? true : false}</li>
    <li></li>
</ul>
`
